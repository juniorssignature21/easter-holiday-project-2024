# Asking the user to input a string
user = str(input("Input a string: "))

# Printing the length of the string
print(len(user))

# Printing the string in uppercase
print(user.upper())

# Printing the string in lowercase
print(user.lower())

# Printing the string in reversed
print(user[::-1])